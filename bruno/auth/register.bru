meta {
  name: Register
  type: http
  seq: 1
}

post {
  url: {{base_url}}/auth/register
  body: json
  auth: inherit
}

body:json {
  {
    "email": "jonsnow@example.com",
    "password": "Password1@",
    "firstName": "Jon",
    "lastName": "Snow"
  }
}

assert {
  res.status: eq 201
}

settings {
  encodeUrl: true
  timeout: 0
}

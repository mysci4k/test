meta {
  name: Forgot password
  type: http
  seq: 6
}

post {
  url: {{base_url}}/auth/forgot-password?email=jonsnow@example.com
  body: none
  auth: inherit
}

params:query {
  email: jonsnow@example.com
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
